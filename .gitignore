# 音效文件现在入库：自己做着玩的项目，音效随代码一起 push 就能上线
# （原先这里排除了 sfx/*.mp3 等，导致线上永远只有内置合成音）

# dist 是部署产物，由 build-dist.ps1 / CF Pages 现场生成，不需要入库
dist/

# 杂项
.DS_Store
Thumbs.db
*.log
node_modules/

# workbuddy 本地笔记不入库
.workbuddy/memory/

# 后端测试的临时副本
.workbuddy/_api_under_test.mjs

# 音效源素材压缩包不入库（切分后的成品在 sfx/ 里）
*.zip
__pycache__/

# 切分脚本的工作目录（源素材已解压备份，不入库）
.workbuddy/_sfx_raw/
.workbuddy/_sfx_split/

# 音乐盒曲目随仓库入库：和 sfx/ 一样，push 即部署上线，朋友打开站点就有歌
# （曾在 2026-09-13 前排除过 music/*，现已放开；原因见 music/README.md）
