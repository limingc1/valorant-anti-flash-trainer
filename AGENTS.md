# AGENTS.md — 背闪模拟器 项目约定

无畏契约（VALORANT）背闪训练器。**单文件 Web 游戏 + Cloudflare Pages Function**，零依赖、零构建。

新会话请先读完本文件，再跑一遍基线测试（见下节）确认起点是绿的。

---

## 改完必做

```
node .workbuddy/smoke-test.js     # 期望 754 条 OK，末行「全部通过」
node .workbuddy/api-test.mjs      # 期望 38 条 OK，末行「全部通过」
node .workbuddy/music-loading-test.js # 44 条：加载、取消、压缩预取和缓存
node .workbuddy/match-start-test.js   # 25 条：双客户端开局、迟到、旧响应及 KV 限制
```

两条都必须全绿。**断言数只增不减** —— 这些数字是回归钉子，一条条对应以前真出过的事故
（每帧重复结算把 KV 额度刷爆、音画不同步、回靶计时虚高、房主看不到对手……）。

如果某次改动确实应该改变一个被钉住的值：改测试，并在提交信息里写清**为什么旧的期望是错的**。
不要为了让测试变绿而删断言。

视觉和手感自动化测不到，改完请让用户在浏览器里打一局确认。

---

## 文件地图

| 路径 | 是什么 |
|---|---|
| `valorant-anti-flash-trainer.html` | **产品本体**。全部 HTML/CSS/JS 内联在这一个文件里 |
| `index.html` | 跳转入口（Pages 输出目录设为仓库根时命中它） |
| `functions/api/room.js` | Pages Function `/api/room`，联机房间；动作 host / join / ready / score / again / leave |
| `.workbuddy/smoke-test.js` | 冒烟测试：Node `vm` + 最小 DOM 桩，跑的是**真实**游戏脚本 |
| `.workbuddy/api-test.mjs` | 后端测试：假 KV 跑完整房间生命周期 |
| `.github/workflows/ci.yml` | CI，跑上面两套 |
| `sfx/` | 自定义音效 wav + 切分脚本 + 说明 |
| `music/` | 音乐盒曲目文件夹（**随仓库入库**，push 即部署；`README.md` 有用法） |
| `build-dist.ps1` | 本地构建 `dist/`（CI 跑同样步骤，平时不用管） |

本地跑游戏：浏览器直接打开 `valorant-anti-flash-trainer.html`。
`file://` 下音效可能加载不了，起个静态服务器即可：`python -m http.server 8000`。

---

## 硬约束（都是踩过的坑，别改回去）

1. **不要引入依赖或构建步骤。** 没有 `package.json` 是故意的 —— 产品必须能双击 HTML 直接跑。

2. **双随机流不能合并。** `contentRnd` / `grand` / `gpickEl` 走种子（内容：特工、轨迹、引爆时机），
   `Math.random` / `rand` / `pickEl` 走真随机（靶点、节奏 `gapFor`）。联机的同种子公平全靠这个划分。

3. **KV 写额度是硬瓶颈。** 免费档 1000 写/天，一整局固定 6 次写，`api-test.mjs` 的 `[F]` 组
   钉死「一局 ≤8 次写」。每日挑战榜再 +1 写/人/天（只有刷新自己当日纪录才写，`[G]` 组钉着）。
   **加写操作之前先算账**，改后端别让它涨。

4. **闪光的出手声不要用 `setTimeout` 或挂帧回调。** 现行架构：`spawnFlash` 算
   `f.audOff`（相对 `f.t0` 的偏移），`updateFlashes` 每帧查 `T-f.t0>=audOff` 播一次
   （`audioPlayed` 闸门）。这样掉帧、暂停（`shiftTime`）都自动跟着走。
   历史上 `setTimeout` 方案和 `<audio>` 方案各走过一次弯路，别回去。

5. **音效播放必须走 WebAudio 预解码缓存**（`loadSfxFiles` 里 `fetch` + `decodeAudioData`
   烘成 `AudioBuffer`，`playFile` 用 `AudioBufferSource`）。用 `<audio>` + `cloneNode(true).play()`
   会每次重新下载解码，实测延迟数秒、连开多只还会攒着一起响。

6. **`sfx/*.wav|mp3` 不要加进 `.gitignore`。** 音频入库是有意的（用户自己录的，无版权问题），
   push 后线上也听得到。单个音效压到几十 KB 以内，别放整段录屏。

7. **DOM 元素 ID 是 JS 的直接查找目标**（约 70 处 `$('xxx')`）。删或改 ID 会让启动时抛
   null TypeError，表现是**黑屏但能听见声音**。动 UI 结构后务必核对所有 ID。

8. **对战中不轮询云端**（省 KV 读额度），只在开局前和等交卷时轮询。
   `pollDelay()` / `startPoll()` 是额度闸门，`cloudGaveUp()` 是 429 降级入口。

9. **音乐盒的音频文件入库**（2026-09-13 起，与约束 6 的 `sfx/` 同策略）。
   `music/*` 已不再被 gitignore —— push 即部署，线上和朋友的音乐盒里直接就有曲子。
   别按「版权音频不该入库」的老印象把它加回 `.gitignore`。
   技术侧三条别改：
   - 播放走 WebAudio 预解码（同约束 5），但**只缓存当前这首**的解码结果
     （3 分钟曲子解码后约 60MB PCM，整张歌单全缓存会吃光内存）；
   - 「出手声压低音乐」（duck）在 `musicTick` 里做帧内插值，别改成 `setTimeout`；
   - IndexedDB 不可用（vm 测试环境/老浏览器）时必须优雅降级成只有文件夹曲目。
   - 歌单先读小 manifest，不批量下载/解码取时长；当前曲开始播放后只预取下一首压缩数据（≤25MB），
     不缓存下一首 PCM。切歌可取消旧下载，过期解码结果不得启动播放；错误需用户重试，不能每帧重拉。

## 联机开局可靠性（2026-09-16）

- `startAt` 迟到不得被简单丢弃：给出“同步较慢”提示并补 3 秒倒计时，确保完整回合。
- 倒计时使用单调时钟，等 rAF 帧再开始；清理菜单/结算遮挡，指针锁失败必须有可点击重试提示。
- `revision` 随已有 KV 写递增，不增加写次数；客户端过滤旧版本/旧轮次/退出房间前发出的响应。
- KV 不是强一致存储：跨地区读延迟、并发 read-modify-write 丢更新仍可能发生；revision 不是事务，
  不能声称解决了这些问题或保证严格同时开局。等待过久可显式选同房码离线开局，双方口头核对。
- 准备 POST 超时结果未知时只查询，不自动重发写；对战中仍不轮询，整局仍 6 次 KV 写。

---

## 设计取向（和「更还原游戏」冲突时的取舍）

- **擦闪（`GRAZE_COVER=0.20`）是故意的宽容，不是 bug。**
  布雷奇锥角 70° 比屏幕半 FOV（103°/2=51.5°）还大，51.5°~70° 这圈屏幕上没有光点却仍按角度判被闪。
  用户明确要求：白一下（零点几秒）算背闪成功，**不断连击**。别按「真实游戏就是这样」把它改严。
  要调只动 `GRAZE_COVER` 一个常量。

- **回靶计时的起点 = 「最早能出手的时刻」**（`reflickStart()`），不是引爆瞬间、也不是白屏完全散尽。
  白屏按 `shoot()` 吞弹线 0.35 倒推，近视按 `nearFog>0.15` 倒推。

- **闪光防护（`cfg.flashGuard`，2026-09-18）默认开启**：这是「回放设置里的闪光防护」那种回放式表现 ——
  被闪时**场景保持可见**，只叠一层淡白雾（`drawFlashGuardWash`）+ 准星正上方的**白色**图标与恢复圆环
  （`drawFlashGuardIcon`）。用户明确要求：默认开启、图标白色、固定在准星上方**且不跟随鼠标/视角**。
  **只覆盖「白屏」类的闪**：普通闪光与丢丢电浆。**蕾娜之眼是近视，用户明确要求不做护眼** ——
  `flashGuardState()` 不读 `nearUntil`，`drawNearsight()` / 近视藏靶 / `surf()` 的近视雾也不看 `cfg.flashGuard`，
  它始终是原来的紫雾 + 藏靶（曾按「近视也算致盲」统一防护，被用户否掉）。
  四条别改：
  - **只改绘制，绝不碰逻辑量**。`blindAmount` / `dizzyBlindAmount` / `fogNear` 同时驱动
    `shoot()` 的吞弹线（0.35 / 近视 0.15）、藏靶与 `reflickStart()` 回靶起点；
    防护一律在绘制端映射（`flashGuardState()` 只读计时），否则等于偷偷降低难度。
  - **关掉整画布的 CSS 模糊与增亮**（原版是 `blur+brightness` 到 1.7×）：只换白屏颜色的话，
    这层增亮会把灰白雾重新提亮，等于没防护。
  - **白雾画在准星下面、图标画在准星上面**（`render()` 里 wash → crosshair → icon）：
    白雾盖住准星就没法瞄，图标被准星穿过就读不出来。
  - 图标用深色底盘（`rgba(16,26,34,0.42)`）托底是必要的 —— 白线压在白雾上会糊掉；
    底盘半径 19 < 圆环 24，读起来仍是「白色图标」而不是深色胸章。
  防护是**本地设置**：不进 `packSync()`、不进 `DAILY.applied/snap`，房主与每日挑战都不得覆盖它
  （房主同步护眼等于替对手改画面）。钉子见 smoke-test `[55]`（含「开关两侧致盲/计分/回靶完全一致」
  与「近视像素不受开关影响」）。

- **致盲按 `cover` 分档**（背掉锥角的比例：正脸 1、擦边 0），同时决定白多久和白多重。

- **蕾娜之眼也有擦边宽容档（`EYE_GRAZE_COVER=0.30`，比闪光的 0.20 更宽）**：贴锥角边缘瞥到
  依然近视，但**不断连击**；击毁紫眼还会返还致盲前的连击。用户实测反馈眼睛太容易断连击后
  加的，别按「更还原」改严。

- **维斯（`vyse`）是放置型，四阶段：放置液滴 → 蓄势花蕊 → 突然绽放 → 引爆。**
  `shape:'rose'` + `noPath`，位置固定在拱门两侧实墙面（`z=ROOM.zW-0.05`）。
  时间轴由三个时间戳驱动（**都在 `shiftTime` 里平移**，漏掉 `bloomAt` 会让暂停恢复后立刻绽放）：
  `t0` 放置 → `bloomAt`（= t0 + 随机延迟，`delay:[2.5,6.0]`×难度系数）→ `popAt` = `bloomAt+bloomDur`。
  蓄势时长走 `hash01(闪光序号 ^ 房间种子)`：**不消耗内容流**（用 `grand()` 会把后面所有特工的
  同种子序列整体错位，联机同种子公平就废了），同房间两人算出的延迟仍然一致。
  绘制：`drawRoseBud`（半透明花蕊 + 呼吸微光，因为它是可击毁目标，不能真藏到看不见）→
  `drawRose`（`roseStage()` 切绽放/亮光两段，开满后渐亮紫光才是「要炸了」的预警）。
  基准半径 `R=rr*1.15`（1.95 / 1.50 两版实测都偏大，已按用户反馈两次收小）。
  引爆是**紫色强光**（`pops` 里带 `rose:true`，走 `orbSprite('165,65,255')` + 紫环 + 白芯）。
  **可击毁（`destructible` + `hp:1`，一击即毁）**：蓄势期打掉 = 终止生成，绽放期打掉 = 取消爆闪，
  两种情况都**绝不会再闪**；击毁算一次成功应对（`st.dodges++` + 击毁分），
  否则「打掉了却拉低躲闪率」会很别扭。蓄势期不计入 `visAt/dodgeAt`，
  不然玩家在蓄势时随手转身就把「转身反应」统计成 0ms。
  **绘制坐标坑（真出过事故）**：`drawRoseBud` 里在 `ctx.translate(sx,sy)` **之内**调 `glowDot`
  必须用局部坐标 `(0,0)`。写成 `(sx,sy)` 会被平移叠加成 `(2sx,2sy)`，在花蕊右下画出一颗
  幽灵般的淡紫小白点（`rgb(236,212,255)` 系、约 2px）——只有维斯会犯，因为只有这里在平移内调 glowDot。
  回归钉子见 smoke-test `[38]`：它追踪 translate/save/restore 累积后比对**有效屏幕位置**，
  只记原始参数是抓不到这个 bug 的（两种写法参数看起来一样）。
  **「边缘提示」（屏幕边缘指方向的小箭头）已按用户要求整体删除**（2026-09-14）：
  函数、`cfg.edge`、设置页开关全部移除。要找回就去 git 历史里翻 `drawEdgeHints`。
  **音效时机（2026-09-16 更新）**：`sfx/vyse_pop.wav` 在花开始旋转绽放（`bloomAt`）时播放，
  不再等完全绽放/引爆。`bloomAudioPlayed` 一次性闸门防止引爆重复播放；掉帧跨过两阶段也只播一次。
  放置仍走 `THROW_SYNTH.vyse` 合成音；录音文件名保留，避免资源路径变化。
  不用 setTimeout，暂停/排期平移后跟随更新的 `bloomAt`；蓄势期击毁不会播放绽放音。

---

## 坐标空间与异步的两个坑（都真的出过事故）

- **拱圈与门柱必须共用 `ARCH_FRAME` 截面与连接边**：门洞 `ARCH/ARCH_POLY` 是判定数据，不随装饰调整。
  旧拱圈宽 0.38、门柱宽 0.29，且内退外凸、端点越过半圆，导致两侧接头突兀。现在两者同宽同深，
  起拱点复用同一顶点；弧形石材逐小条带贴图，不能整块扇形三角化跨进门洞。
  倒角沿内外长边固定宽度，不能按柱高比例缩进（会画成厚亮柱头）；前墙腰线、踢脚线止于门框外沿，
  且先于门框绘制，防止斜视时横盖柱身。回归见 smoke-test `[56]`。

- **墙面贴图不能按视角切换整墙两片 / 细分网格**：旧 `zmax/zmin<1.06` 快路径在尽头砖墙
  左右约 4.283° 时硬切换，轻微转头就让砖缝突然跳位。保持固定世界 UV 网格：砖墙与前墙装饰
  最多 70 格，素灰泥最多 40 格；近裁面同时插值 UV。smoke-test `[54]` 检查跨旧阈值的纹理
  **内点**连续性（只检查三角形顶点抓不到这个问题）。
  前墙装饰烘入独立 `facade` 缓存，基础 `plaster` 不带窗户或铭牌；四张 1024×384 位图合计约 6MB。
  装饰仅在 `|x|>=3.3` 的实墙侧边，门洞周围留给技能预警，不改变遮挡判定或消耗游戏随机流。

- **canvas 的位图像素 ≠ CSS 像素 ≠ 世界坐标**，混用必出偏差：
  - 花蕊：`drawRoseBud` 在 `ctx.translate(sx,sy)` **之内**调 `glowDot` 必须用局部 `(0,0)`
    （写成 `(sx,sy)` 会被叠加成 `(2sx,2sy)`，画出一颗幽灵淡紫小白点）。钉子见 `[38]`。
  - 走势图吸附：位图 560×150，CSS 是 `width:100%`（实测 498×150）。鼠标事件给的是 CSS 像素，
    必须 `× cv.width/rect.width` 换算成位图像素再比 —— 否则吸附点整体横移（最右点偏 48px
    > 20px 吸附半径 = 完全不响应，实测被报「移到圆点上大多数不显示」）。钉子见 `[40]`。
- **每日榜单 `renderDaily()` 必须自己设 `DAILY.date`**：它原本只在 `dailyStart()` 里赋值，
  新开页面是空串，于是守卫 `DAILY.date===dailyKeyDate()` 永远不成立、拉回来的榜单被丢弃
  （表现「等多久都不出榜单」）。同一份榜单 20 秒内不重复拉（切菜单页不再每次干等），
  拉不到时给明确文案而不是停在「加载中」。钉子见 `[39]`。
  **本地测试没有后端**（`python -m http.server` 没有 Pages Function），榜单必然显示
  「暂时拉不到」——那是正常的，不是 bug；要看真榜单得上部署站点。

---

## 部署

push 到 `main` → Cloudflare Pages 自动部署，约 1 分钟生效。**提交即上线，没有单独的发布步骤。**

- 联机需要 Pages Function + KV（绑定名 `ROOMS`）；`file://` 或未绑定 KV 时自动降级成离线对战。
- 本仓库的 `.git/config` 里配了 git 代理 `http://127.0.0.1:10808`（仓库本地配置，跟着文件夹走）。

---

## 已知待办

- **实时比分显示**（现在对战中故意不轮询）。动手前先算 KV 写额度：每 5s 上报一次、60s 一局
  = 每人 12 写、一局 24+ 写，1000/天只够 40 局 —— 大概率该换 **Cloudflare D1**（免费 10 万写/天）。
- **盖克（`gekko`）丢丢已实现，默认关闭**：飞行 → 扫描 → 锁定前摇 → 发射电浆 → 命中。
  `lockAt/fireAt/popAt` 都走游戏时钟和排期平移，电浆命中才是 `flashImpactAt`。
  丢丢一枪击毁；发射前击毁算成功应对，发射后击毁**不取消独立的 `dizzyPlasmas`**、不给躲闪分。
  不读玩家朝向，不走擦闪或转身反应统计；蓝色电浆遮屏独立于普通白屏，持续 1.8 秒（末 0.45 秒淡出）。
  `cfg.vis` 不隐藏丢丢（必须能看见才能打）；暂停平移本体阶段、电浆、蓝幕与账本 `t/clear`，重开清空。
  **飞行与时序**：`pathAt` 对丢丢单独做起点到终点的线性插值，不走 Catmull 曲线或抛物弧。
  飞行中就扫描：出现后固定 0.25s 锁定，再前摇 0.35s×难度系数（普通约 0.60s 发射）。
  电浆起点取计划发射时刻的本体位置，不能取尚未飞到的 `home`。
  模型为有明暗、高光和前后膜分层的红色球体包住小兽；盖克拖尾仅保留 0.18s，强度系数 0.16。
  **音效分工**：原始 `_sfx_split/gekko_throw.wav` 在 0.25s 处分段，前段为
  `sfx/gekko_throw.wav`（出现），后段为 `sfx/gekko_lock.wav`（锁定起播放，包含后续发射声）。
  该后段已加载时不再叠加合成发射声；素材缺失仍可合成兜底。
  `sfx/gekko_pop.wav` 继续用于电浆命中，原录音保持不变。
  所有触发均走游戏时钟与一次性闸门，不用 setTimeout。
  **音频生命周期（踩过两次，别再改回去）**：
  - 规则是「**电浆只要射出去了，就必须有音频**」：发射**前**击毁 → 立即静音、不播命中声；
    发射**后**击毁 → 录音照常播完（后段本身就是发射声），命中时再叠加一次 `gekko_pop`。
    不要一见到「本体死了」就 stop —— 那会把发射声一起掐掉（实测被报「打慢一步就没声音」）；
    反过来也不要无条件停，发射前击毁必须静音（实测被报「打掉了还有声音」）。
  - 句柄**双重登记**：各自的 `f.audioSources`（发射前击毁只静音这一只）+ 全局 `dizzyAudios`
    （重开统一收尾）。只挂本体不够 —— 本体被击毁后从 `flashes` 移除、电浆命中后又从
    `dizzyPlasmas` 出队，而录音最长约 2s，那之后重开就没人认领，会跨局继续响。
  - 命中时刻**不**切断本体录音（早期版本在此处 stop，正是「发射声消失」的根因）。
  致盲纹理保持环形拼贴版（2026-09-15 试过连续流线，用户对比后认为不如原版，已还原）：
  缓存整张位图，只做整体缩放与不透明度，不改变 1.8s 致盲或末段淡出判定。
  球内小兽按「圆润萌系」画：近圆胖身 + 两只小圆耳 + 短尾鳍 + 小圆手 + 腮红，
  黄虹膜红瞳单眼带双高光，锁定后瞳孔收窄、嘴巴聚起青色电浆。
  尾巴必须收在球面内（早期版本会戳出外膜），小兽按 0.70/0.72 缩放后有点偏心平移。
  **每日挑战已加入维斯与盖克，并新增 5 条「特工专场」规则**（2026-09-15 用户要求）：
  `DAILY_BASE.agents` 现为 8 个特工；`DAILY_MODS` 共 11 条（原 6 条 + 丢丢疯潮 / 荆棘玫瑰 /
  群鸟乱舞 / 一枪一个 / 三色闪光）。专场规则考的是「这几位打不掉就必被闪」的题型。
  **限定特工的表必须写全所有特工键**：合并是 `Object.assign(基础表, 规则表)`，
  漏键会继承基础表的 `true`，于是本该被排除的特工会漏进来（踩过）。新增特工时
  用 `dailyOnlyAgents([...])` 生成整表，别手写；smoke-test 按**实际抽签结果**逐条钉住。
  改规则池会改变当日及之后的挑战序列，全网榜单以当天起的种子为准（本地今日最佳按日期隔离）。
  回归钉子见 smoke-test `[43]`～`[47]`（含真实帧循环的混合排期测试）。
- 靶点布局两人不保证完全一致（各自打掉各自补），这是异步同种子对战的固有权衡，**别当 bug 修**。

---

## 闪光引爆间隔保护（`reserveFlash`）

玩家真正要应对的是**引爆那一刻**。维斯加入随机蓄势后，放置顺序与引爆顺序不再一致，
于是会出现「两发同时炸」或「刚被炸完下一发就贴脸」—— 那不是技术问题，是没得反应。

`IMPACT_GAP=[1.2,0.9]`（普通 / 困难）：任意两次引爆至少隔开这么多秒，并且**任何闪光的
预警开始时刻**（出手 / 绽放 / 睁眼）都不许落在别人引爆后的恢复窗口里。**两个方向都要挡**：
新闪光挤到别人「绽放开始」前面同样是违例 —— 只挡一个方向会漏出连闪，实测踩过。

调整顺序按「对画面影响最小」：
1. 撞上的是一枚**还在蓄势的维斯花蕊** → 延长它的花苞（蓄势本来就随机，玩家看不出）；
2. **自己还没出手** → 整体平移（`shiftFlashPlan`：t0/绽放/睁眼一起挪，飞行与预警时长不变）；
3. 对方已经在飞行 / 已经开始绽放 → **谁都不动**。宁可挨近一点，也不让花「开完不炸」、
   或者让飞一半的闪光突然变速 —— 那是玩家听声判断转身的根据。

三条别改：
- 判定只读排期账本 `impactSlots`（`{t,dur,f}`），**不读 `popped/dead`、不参考玩家是否被闪**：
  玩家打掉玫瑰不能改变后面的题目顺序，否则联机两人、每日挑战全网的排期会分叉。
- `flashSeq` **每轮归零**（`resetStats` 里）：它是蓄势时长哈希的输入，不归零则两个玩家会因
  入场前的练习次数不同而算出不同延迟（组队前各自练了几发就错开了）。
- 每日挑战的节奏同样走种子（`gapFor` 用 `DAILY.seed`），否则全网「同一串闪光」不成立。

`spawnFlash(key, scheduled, at)` 的第三个参数是**计划出手时刻**（不是当前帧 T）：排期按
计划时刻推算，掉帧不会累积漂移；未到 `t0` 的闪光不画、不统计、也不可被击毁
（`activateFlash` 的 `announced` 闸门保证只登记一次）。
钉子见 smoke-test `[41]`。
